#!/usr/bin/env sh
USER=$(whoami)
OBNAM=$(which obnam)
LOGGER=$(which logger)
echo "starting"
if [ -f /home/$USER/.ssh/config ] && [ -f /home/$USER/$USER.home.obnam.conf ]; then
    $OBNAM backup $HOME --config=/home/$USER/$USER.home.obnam.conf
else
     logger CONFIG: obnam wrapper for $USER failed
fi
exit $?;
